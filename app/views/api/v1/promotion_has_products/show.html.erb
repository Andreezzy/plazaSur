<p id="notice"><%= notice %></p>

<p>
  <strong>Promotion:</strong>
  <%= @promotion_has_product.promotion %>
</p>

<p>
  <strong>Product:</strong>
  <%= @promotion_has_product.product %>
</p>

<%= link_to 'Edit', edit_promotion_has_product_path(@promotion_has_product) %> |
<%= link_to 'Back', promotion_has_products_path %>
