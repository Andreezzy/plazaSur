<%= form_for(chance) do |f| %>
  <% if chance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chance.errors.count, "error") %> prohibited this chance from being saved:</h2>

      <ul>
      <% chance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :products_id %>
    <%= f.text_field :products_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
