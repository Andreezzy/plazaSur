<%= form_for(atp_impost) do |f| %>
  <% if atp_impost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atp_impost.errors.count, "error") %> prohibited this atp_impost from being saved:</h2>

      <ul>
      <% atp_impost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :impost %>
    <%= f.number_field :impost %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
